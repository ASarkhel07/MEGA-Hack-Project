import 'package:flutter/material.dart';

//height, returns sized box with that height; same for width
Widget addVerticalSpace(double height){
  return SizedBox(
    height:height
  );
}

Widget addHorizontalSpace(double width){
  return SizedBox(
    width:width
  );
}